# إصلاح نتائج اختبار رحماء — 10 سبتمبر 2026

## القرار

**PASS لإصلاح العيوب المثبتة ضمن نطاق الاختبارات المنفذة؛ HOLD للإطلاق العام والتغطية الشاملة المتبقية.**

| بوابة التحقق | النتيجة |
|---|---|
| إعادة تشغيل الترحيلات | 60 ترحيلًا مع فحوص ما بعد التطبيق |
| الخادم والأمن عبر حسابات حقيقية معزولة | 33/33 ناجحة |
| واجهة Linux | 33/33 ناجحة |
| واجهة Android API 35 | 33/33 ناجحة |
| الاختبارات السابقة | 21/21 ناجحة |
| Flutter analyze وبناء Android debug | ناجحان |
| حزم Pub في OSV | 110 حزم، صفر تنبيهات مسجلة |
| التخلص من قاعدة الاختبار وملفات الاعتماد | ناجح |

## المرجع والنطاق

طلب التغييرات: https://github.com/msorori-mh/Rohamaa/pull/11

النسخة المستهدفة بالتحقق: `83ae3db29c0a5301143aec3f315e33d17f41c12a`، فرع `codex/isolated-e2e-tests`.

التشغيل الموسع: https://github.com/msorori-mh/Rohamaa/actions/runs/34422822100

التحليل والاختبارات السابقة وبناء APK: https://github.com/msorori-mh/Rohamaa/actions/runs/34422822082

هذه مرحلة إصلاح للعيوب المثبتة في الجولة السابقة وإعادة اختبارها. جميع الكتابات التشغيلية في الجولة تخص قاعدة Supabase مؤقتة وحسابات اصطناعية. لم تُطبق هذه التغييرات على الإنتاج ولم تُدمج طلبات التغييرات.

## ما تغير

| الخلل السابق | الإصلاح |
|---|---|
| إنشاء مساهمة معتمدة من مستخدم عادي | سياسة الإدخال تشترط pending وغياب verified_by وverified_at |
| إدخال تبرع أو احتياج بحالة نهائية | سياسة الإدخال تقبل الحالة الابتدائية submitted فقط |
| كشف بصمات رموز التوصيل | إزالة SELECT العام ومنح قراءة الأعمدة التشغيلية المسموحة صراحة، مع استمرار RLS |
| السماح للموظف قبل تغيير كلمة المرور المؤقتة | فحص force_password_change في صلاحيات الخادم مع الإبقاء على شرط نشاط الموصل وعدم إيقاف الحساب |
| تعطل توليد الرموز وتسجيل الشريك | ضبط مسار الدوال المحددة ليشمل مخطط extensions حيث توجد pgcrypto |
| قبول الرمز الفارغ أو محاولات غير محدودة | رفض NULL والصيغ غير الصالحة، صلاحية 10 دقائق، خمس محاولات، وإعادة ضبط العداد فقط عند إصدار المالك رمزًا جديدًا |
| تعطل استلام المخزون | إبقاء SECURITY INVOKER، وإضافة صلاحية تحديث وصفّ إداري، وحارس يمنع صاحب التبرع من تغيير الحالة مباشرة ويثبت وقت التحديث من الخادم |
| إكمال المعالجة قبل بدئها | اشتراط in_progress قبل الإكمال، مطابقًا لتسلسل الواجهة |
| قبول ملفات غير صور أو كبيرة | أنواع JPEG وPNG وWebP فقط وحد أقصى 5 ميغابايت مع اختبار رفع صورة صحيحة |
| أخطاء setState أثناء التحديث | تحويل 16 دالة تحديث من التعبير الذي يرجع Future إلى كتلة متزامنة، وإضافة اختبار تحديث الشاشات |
| تعطل تخطيط أزرار المخزون والإسناد | استبدال الحد الأدنى غير المحدود لعرض الأزرار بعرض أدنى محدود |
| تعطل التحقق عند غياب الموقع | الموقع دليل اختياري؛ انتهاء مهلة GPS أو عدم توفره لا يمنع التحقق من الرمز |
| التخلص المبكر من متحكم نص النافذة | ترك حقول نوافذ الرمز والبلاغ تدير متحكماتها حتى اكتمال إغلاق النافذة |

## ملف الترحيل

`supabase/migrations/20260909235810_e2e_security_closure.sql`

ولّده Supabase CLI 2.117.0 داخل CI، ثم ثُبت الملف الناتج في المستودع. أصبح الإجمالي 60 ملف ترحيل. لا يحذف الترحيل بيانات تشغيلية أو يعيد كتابة تاريخ الترحيلات السابقة.

قبل تطبيقه على بيئة قائمة، يجب جرد رحلات التوصيل والرموز الحالية. الرمز السابق الذي لا يملك وقت انتهاء سيُرفض؛ يستطيع صاحبه إصدار رمز جديد من المسار المسموح. قيود التخزين الجديدة لا تحذف ملفات موجودة. الحسابات ذات كلمة المرور المؤقتة تحتاج تغييرها قبل الإجراءات المحمية. الاستعادة المفضلة إصلاح أمامي؛ إعادة فتح الصلاحيات السابقة تعيد الثغرات.

## التحقق

ثبت على النسخة المعروضة أعلاه نجاح 33 من 33 اختبار خادم وأمن، و33 من 33 اختبار واجهة Linux، و21 اختبارًا سابقًا، والتحليل وبناء APK المعتاد. فُحصت 110 حزم Pub دون تنبيهات مسجلة. إصدار Flutter في ملف الأدلة هو 3.47.3. نجح أيضًا 33 من 33 اختبار واجهة Android API 35 بعد تثبيت التطبيق ومنح أذونات الموقع قبل تشغيل الاختبارات. التشغيل السابق انتهى بصفر اختبارات بسبب تعطل جلسة البدء، ولم يُحتسب نجاحًا. نجحت بوابة التشغيل النهائية وتنظيف قاعدة الاختبار وملفات الاعتماد.

أضيفت رحلة واجهة تربط إنشاء المطابقة وقبولها بتهيئة بيانات عبر حسابات حقيقية، ثم تختبر من الواجهة إسناد المدير، وقبول الموصل، وإدخال رمز الاستلام، وبدء التسليم، وإدخال رمز التسليم، وتتحقق من إغلاق الاحتياج. إعطاء أذونات الموقع في المحاكي إعداد اختبار صريح؛ لا يمثل اختبار نافذة أذونات الهاتف.

## الحدود المتبقية

لا تعني هذه المرحلة تغطية كل نموذج وكل حالة في التطبيق. تبقى تفاعلات متفرعة أخرى، وGoogle OAuth على الأجهزة، وروابط التطبيق الموقعة، وأذونات الجهاز، وPush أثناء الإغلاق، وiOS، والتعافي الشامل من انقطاع الاتصال وإتاحة الاستخدام بوابات منفصلة. فحص OSV يخص حزم Pub، ولا يشمل Gradle أو الثغرات غير المنشورة. قيود التخزين تتحقق من نوع MIME والحجم؛ لا تمثل فحصًا للبرمجيات الخبيثة أو التحقق من محتوى كل ملف.

فحص مستشار Supabase الإنتاجي للقراءة فقط ما زال يعرض 43 دالة SECURITY DEFINER قابلة لاستدعاء المستخدم المصادق عليه، وحماية كلمات المرور المسرّبة غير مفعلة. قابلية استدعاء دوال الأدوار المقصودة ليست وحدها إثبات تجاوز صلاحيات، لكن النتيجة ليست شهادة أمان شاملة. لم يُغير هذا الفحص إعدادات الإنتاج.

مراجع الصلاحيات والتنبيهات: [صلاحيات الأعمدة](https://supabase.com/docs/guides/database/postgres/column-level-security)، [تنبيه الدوال ذات صلاحيات المالك](https://supabase.com/docs/guides/database/database-linter?lint=0029_authenticated_security_definer_function_executable)، [حماية كلمات المرور المسرّبة](https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection).


قراءة الإنتاج بتاريخ 10 سبتمبر: صفر مهام توصيل وصفر مهام تحمل بصمات رموز. CREATE غير مسموح للمستخدم المصادق عليه في public أو extensions، ولا للزائر في extensions. لم تتغير هذه القيم بكتابة إنتاجية في هذه المرحلة.

## فهرس اختبارات الخادم والأمن

هذه أسماء الحالات القابلة لإعادة التشغيل في `tests/e2e/run_api.py`، وليست ادعاءً بتغطية كل احتمال:

- `test_auth_sessions_are_real_and_owned`
- `test_profiles_cannot_be_read_by_another_user`
- `test_role_escalation_is_denied`
- `test_address_read_and_update_are_owner_scoped`
- `test_donation_and_need_create_read_cancel`
- `test_cannot_insert_item_owned_by_another_user`
- `test_item_initial_status_is_server_controlled`
- `test_owner_cannot_update_workflow_after_inventory_grant`
- `test_contribution_creation_and_idempotent_admin_verification`
- `test_contribution_cannot_be_created_as_verified`
- `test_contribution_invalid_amount_is_rejected`
- `test_complete_delivery_lifecycle_and_privacy`
- `test_null_pin_does_not_change_delivery_state`
- `test_courier_cannot_read_pin_hash_columns`
- `test_pin_attempt_limit_and_expiry`
- `test_courier_decline_and_reassignment`
- `test_temporary_password_cannot_accept_delivery`
- `test_inactive_courier_cannot_accept_delivery`
- `test_suspended_account_cannot_create_need`
- `test_service_offer_request_matching_and_consent`
- `test_partner_registration_and_owner_isolation`
- `test_notifications_are_owner_scoped`
- `test_storage_rejects_other_user_path`
- `test_storage_rejects_non_image_upload`
- `test_storage_accepts_owner_image`
- `test_storage_rejects_oversized_image`
- `test_admin_queues_are_denied_to_regular_users`
- `test_concurrent_courier_acceptance_has_one_winner`
- `test_reviewed_need_publish_hide_republish`
- `test_inventory_grade_routes_and_work_orders`
- `test_account_deletion_request_is_idempotent`
- `test_edge_staff_creation_rejects_regular_user`
- `test_staff_password_change_flow`

## طبيعة تغطية الواجهة

- 13 مسارًا للمستخدم: الرئيسية، العروض، الاستلام والتسليم، الحساب، الإعداد الأول، إنشاء التبرع، إنشاء الاحتياج، الاحتياجات الموثوقة، عرض خدمة، طلب خدمة، خدماتي، الشركاء، والمعلومات القانونية. يتحقق الاختبار من فتح الشاشة وتحديثها إذا توفر التحديث؛ لا يرسل كل نموذج متفرع.
- لوحة الإدارة و14 أداة: المخزون، الاحتياجات الموثوقة، مطابقة الأشياء، الخدمات، جاهز للتوصيل، متابعة التوصيل، المخاطر، بلاغات الخدمات، الشركاء، المساهمات، التقارير، المناطق، إنشاء الموظف، والأدوار.
- شاشة الموصل، شاشة المشرف، ومنع المستخدم العادي من فتح الإدارة.
- عملية إنشاء مساهمة من الواجهة والتحقق من حفظها pending.
- عملية إسناد مدير وقبول موصل ورمزي استلام وتسليم من الواجهة مع التحقق من النتيجة في الخادم. تهيئة التبرع والاحتياج والمطابقة وقبول العرض تتم عبر API بالحسابات المختصة.

عدد حالات الواجهة 33 لكل منصة؛ لا تجمع كأنها 66 سيناريو أعمال مختلفًا، فالسيناريوهات نفسها تُعاد على منصتين.

## حزمة الأدلة

`Ruhamaa_Security_Closure_Evidence_2026-09-10.zip`

SHA-256: `df10a1c715517063488409d88d24a536f6e8ba140a1cd3094a62e5fc174413df`

الحزمة نسخة من artifact `isolated-e2e-results` رقم `10131952462` للتشغيل النهائي. تحتوي سجلات الخادم وJUnit والتحليل والاختبارات وLinux وAndroid وسجل الجهاز ونتيجة الاعتماديات. لا تحتوي ملفات حسابات الاختبار الخاصة أو APK محقونًا بها.
